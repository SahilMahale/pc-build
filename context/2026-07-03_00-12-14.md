# Session: Go Price Tracker Restructure + Oracle Cloud Deployment Research

**Date:** 2026-07-03 00:12:14
**Tokens used:** 96,260
**Branch:** main

## Git State
```
d530aa3 Restructure to root-level main.go, add progress bar, mobile responsive, Docker support
7e14225 Fix last pass timestamp to show local timezone
c672f24 Add IBM Cloud Code Engine deployment to Makefile and README
79a6b53 Add deployment discovery documentation
```

## Key Achievements
- Restructured project from app/ to root main.go + internal/ package structure
- Removed Python scraper, migrated to Go-only codebase
- Added real-time progress bar (done/total sources) during scraping
- Implemented relative timestamps ("5m ago") + JS local timezone conversion
- Built mobile-responsive CSS (hides trend/delta columns on small screens)
- Created Containerfile, Makefile, DOCKER.md for containerized deployment
- Researched Oracle Cloud Free Tier deployment options
- Created deployment-discovery.md documenting hosting alternatives

## Active Files Changed
- main.go (new entry point)
- internal/config.go, scrape.go, store.go, web.go
- templates/index.html, prices.html
- Makefile (added IBM Cloud + podman targets)
- README.md (updated deployment sections)
- PROGRESS.md (current architecture)
- deployment-discovery.md (new)

## Critical Decisions
- Switched from stdlib ServeMux to Fiber v2 for web server
- Kept net/http.Client for scraping (separation of concerns)
- Used mutex for thread-safe progress tracking (scraper goroutine updates progDone, web handler reads)
- HTMX polling: 2s during scraping, 30s idle
- Timestamps: show relative ("5m ago") + absolute (local timezone via JS)
- Docker/Podman: single-stage build, Alpine runtime
- IBM Cloud Code Engine: too expensive for 24/7 ($19.78/month), recommended Oracle Cloud Free Tier instead

## Oracle Cloud Deployment Context
- User attempting to provision VM.Standard.A1.Flex (ARM, 1 OCPU, 6GB RAM)
- Hit "Out of capacity" error in AD-1 (single availability domain region)
- Upgraded to Pay As You Go account (in progress)
- Created VCN (vaayuo-vcn-1) and public subnet (vaayuo-vcn-1-subnet-public)
- Generated SSH keys (2 devices: personal + work)
- Researched auto-retry scripts (hitrov/oci-arm-host-capacity)
- Discovered Always Free allows: 2× AMD E2.1.Micro OR up to 4 ARM OCPUs total
- Can have both AMD + ARM simultaneously (within 200GB boot volume limit)

## Blockers / Open Issues
- PAYG upgrade pending ("Your upgrade is in progress" message, typical wait: 5-30 mins to 4 hours)
- ARM capacity unavailable in user's region (only 1 AD)
- Options: 
  1. Keep retrying AD-1 manually
  2. Provision AMD E2.1.Micro now (instant), migrate to ARM later
  3. Set up auto-retry script via GitHub Actions
  4. Switch to multi-AD region

## Next Steps
1. Wait for PAYG upgrade confirmation email
2. Decide: provision AMD E2.1.Micro now vs keep retrying for ARM
3. If provisioning AMD: deploy pc-price-server, set up Cloudflare Tunnel for public access
4. If waiting for ARM: set up auto-retry script or manual periodic retries
5. Once instance provisioned: deploy via Docker, configure Cloudflare Tunnel with DuckDNS subdomain

## Compact Context for Next Session
**Current task:** Oracle Cloud instance provisioning (ARM or AMD Always Free)

**Key files:**
- Containerfile (ready for deployment)
- deployment-discovery.md (hosting research)
- config.yaml (scraper config)

**Oracle Cloud context:**
- VCN: vaayuo-vcn-1 (10.0.0.0/16)
- Subnet: vaayuo-vcn-1-subnet-public (10.0.0.0/24)
- SSH keys: 2 added (personal + work)
- Target shape: VM.Standard.A1.Flex (1 OCPU, 6GB) or VM.Standard.E2.1.Micro (AMD fallback)
- PAYG upgrade: pending

**Deployment plan:**
1. Oracle Cloud VM (Always Free)
2. Docker deployment via Makefile
3. Cloudflare Tunnel for public HTTPS access
4. DuckDNS subdomain (free)

**pc-price-server specs:**
- Go 1.26+, Fiber v2, HTMX
- SQLite (single-writer constraint)
- Resource usage: ~50-100MB RAM, <5% CPU average
- Fits easily on E2.1.Micro (1GB RAM) or A1.Flex (6GB RAM)
