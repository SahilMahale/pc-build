# Session: Oracle VM Online + Deployment Runbook + Recovered Missing internal/ Package

**Date:** 2026-07-03 22:24
**Tokens used:** ~135,000 (estimate)
**Branch:** main

## Git State
```
9e1f7f4 Add internal/ package — was silently excluded by old .gitignore
8cf489f Add deployment runbook; stop gitignoring internal/ source
79a6b53 Add deployment discovery documentation
c672f24 Add IBM Cloud Code Engine deployment to Makefile and README
7e14225 Fix last pass timestamp to show local timezone
```
Untracked: `context/` (session archives — commit-vs-gitignore decision still open)

## Key Achievements
- **Recovered the missing `internal/` package** — the restructure session's
  .gitignore listed `internal/` and `app/` under "build artifacts", so the
  entire Go source package was never committed; the GitHub repo did not
  compile. Fixed .gitignore (8cf489f), committed + pushed internal/ (9e1f7f4)
- Fixed VM SSH hang — manually-created VCN `vaayuo-vcn-1` had no
  `0.0.0.0/0` → Internet Gateway route; user added it, SSH works
- Provisioned Oracle VM confirmed as **A1.Flex ARM (aarch64), Oracle Linux 9**
  — Mac (Apple Silicon)-built podman images run on it natively
- Wrote **DEPLOYMENT.md** — full runbook: podman quadlet (rootless, SELinux
  :Z labels, `database: /data/prices.db` persistence gotcha), cloudflared as
  systemd service (no NGINX needed — cloudflared IS the reverse proxy),
  Cloudflare Access login wall, containerized-cloudflared variant with
  podman network, troubleshooting table
- VM package setup: git/podman/make from base repos; htop/neovim need
  `oracle-epel-release-el9`; zellij via GitHub aarch64-musl binary
- Auto security patches: dnf-automatic (upgrade_type=security,
  apply_updates=yes) + Ksplice (free on OCI) for reboot-free kernel patching
- Earlier same conversation (pre-restructure, already in git history):
  repo tidy, verified all 20 scraper sources, Python v1 checkpoint
  (50a576a), Go v2 in app/ (6beed0d)

## Active Files Changed
- .gitignore (removed internal/ + app/ ignore rules)
- internal/ (committed for the first time)
- DEPLOYMENT.md (new)
- context/ (this archive)

## Critical Decisions
- **Domain:** bought on Porkbun (replaces DuckDNS plan); nameservers to move
  to Cloudflare free plan
- **Architecture: no NGINX, no custom podman network** — cloudflared (host
  systemd service) is the reverse proxy; app container publishes to
  127.0.0.1:8090 only; port 22 stays the only OCI ingress rule
- **Cloudflare Access** (email OTP) will gate the dashboard — it has a
  public "Scrape now" button
- **Registry:** OCIR chosen if/when needed — effectively free on PAYG
  (Object Storage rates, ~30MB image; same-region pulls free). Endpoint
  `<region>.ocir.io`, auth token as password
- **Build-on-server vs CI:** discussed; agreed the better pattern is GitHub
  Actions → build linux/arm64 image → push OCIR → VM pulls with read-only
  token + `AutoUpdate=registry` quadlet + podman-auto-update.timer.
  Pending user decision (needs: OCIR region, repo visibility)

## Blockers / Open Issues
- App not yet deployed on the VM (runbook ready, repo now actually compiles)
- Porkbun → Cloudflare nameserver switch status unknown
- CI/OCIR pipeline: awaiting user's region + public/private repo answer
- context/ folder untracked — commit or gitignore, user's call
- RAM target ₹5,750/stick is ~4× below DDR5-shortage market — alerts will
  never fire until revisited

## Compact Context for Next Session
**Current task:** Deploy pc-price-server to the Oracle VM per DEPLOYMENT.md

**Key files:** DEPLOYMENT.md (runbook), config.yaml (set database:
/data/prices.db in prod), Containerfile, Makefile

**VM:** Oracle Linux 9 aarch64 (A1.Flex), SSH via default ~/.ssh/id_ed25519,
user `opc`, git/podman installed. GitHub repo: SahilMahale/pc-build (clone
via deploy key or HTTPS; VM must use github.com host, not the Mac's
github-personal alias)

**Next steps:**
1. Clone repo on VM, config.yaml → /data/prices.db, `podman build`
2. Quadlet unit + linger (DEPLOYMENT.md §4)
3. Cloudflare: nameservers → tunnel → Access (DEPLOYMENT.md §5-6)
4. Optional: GH Actions → OCIR → auto-update pipeline
